# Public web client. Use http://localhost:3000 for Docker Compose.
VITE_API_BASE_URL=http://localhost:3000

# Node API
API_PORT=3000
ANALYSIS_URL=http://analysis:8000
DATABASE_URL=postgresql://geoready:geoready@db:5432/geoready

# PostGIS (local development only; replace before any hosted environment)
POSTGRES_DB=geoready
POSTGRES_USER=geoready
POSTGRES_PASSWORD=change-me-before-hosting

# Local object storage (optional until file ingestion is implemented)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me-before-hosting
